/**
 * Placeholder for the developer-comparison page. The two-profile compare
 * UI, winner highlighting, and data fetching land in Phase 5.
 */
export default function ComparePage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-16">
      <p className="text-xs uppercase tracking-[0.2em] text-muted">Compare</p>
      <h1 className="mt-2 font-display text-3xl font-semibold tracking-tight">
        Compare developers
      </h1>
      <p className="mt-3 max-w-md text-sm text-muted">
        This route is live — the side-by-side comparison UI arrives in
        Phase 5.
      </p>
    </main>
  );
}
